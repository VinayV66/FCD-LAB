Lex program 
%{ 
#include "y.tab.h" 
%} 
%% 
[0-9]+ { yylval = atoi(yytext); return NUM; } 
.     
%% 
 return yytext[0]; 
Yacc program 
%{ 
#include <stdio.h> 
int temp = 0; 
%} 
%token NUM 
%% 
expr: expr '+' expr { 
printf("MOV R%d, %d\n", temp, $1); 
printf("ADD R%d, %d\n", temp, $3); 
$$ = temp++; 
} 
| NUM { $$ = $1; } 
; 
%% 
int main() { 
printf("Enter expression:\n"); 
yyparse(); 
return 0; 
} 
int yyerror(char *s) { 
printf("Error\n"); 
} 
